
// Menu keys - not stored persistently
module SyncMenu {
	enum {
		START_SYNC,
		SELECT_PLAYLISTS,
		DEBUG_INFO,
	}
}

module SyncDebugMenu {
	enum {
		TEST_SERVER,
		SERVER_DETAIL,
		DONATE,
		REMOVE_ALL,
		LIST_ERRORS,
		PLAYLIST_DETAIL,
	}
}

module PlaybackMenu {
	enum {
		MANAGE_PLAYLISTS,
	}
}