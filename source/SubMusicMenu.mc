
// Menu keys - not stored persistently
module SyncMenu {
	enum {
		START_SYNC,
		SELECT_PLAYLISTS,
		DEBUG_INFO,
	}
}

module SyncDebugMenu {
	enum {
		TEST_SERVER,
		REMOVE_ALL,
		SERVER_DETAIL,
		LIST_ERRORS,
		PLAYLIST_DETAIL,
	}
}

module PlaybackMenu {
	enum {
		MANAGE_PLAYLISTS,
	}
}